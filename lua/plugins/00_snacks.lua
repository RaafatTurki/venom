local icons = require "helpers.icons".icons
local keys = require "helpers.keys"

require "snacks".setup {
  -- picker = {},

  -- scroll = {},

  -- words = {},

  input = {
    icon_pos = false,
    win = {
      border = "single",
    }
  },
}
